import { Check, CircleDashed, Database, FileText, Sparkles } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function ReportGenerationTimeline({ hasAnalysis }: { hasAnalysis: boolean }) {
  const steps = [{ label: "采集比赛数据", detail: "球队状态、赛程与市场信息", icon: Database }, { label: "构建分析特征", detail: "攻防、主客场与近期样本", icon: Sparkles }, { label: "生成模型观点", detail: hasAnalysis ? "已完成模型计算" : "将在赛前任务中生成", icon: CircleDashed }, { label: "整理球探报告", detail: hasAnalysis ? "已保存至比赛分析记录" : "等待分析结果写入", icon: FileText }];
  return <Card className="border-slate-800 bg-[#111827]"><CardHeader><CardTitle className="flex items-center gap-2 text-base text-white"><Sparkles className="h-4 w-4 text-violet-300" />AI生成过程</CardTitle><p className="text-xs leading-5 text-slate-500">报告由结构化数据输入、模型计算和内容整理三个阶段组成。</p></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-4">{steps.map((step, index) => { const Icon = step.icon; const complete = index < 2 || hasAnalysis; return <div key={step.label} className="relative rounded-2xl border border-slate-800 bg-slate-950/30 p-4"><div className="flex items-center justify-between"><span className={`flex h-9 w-9 items-center justify-center rounded-xl ${complete ? "bg-emerald-500/15 text-emerald-300" : "bg-violet-500/15 text-violet-300"}`}><Icon className="h-4 w-4" /></span>{complete ? <Check className="h-4 w-4 text-emerald-300" /> : <span className="text-[10px] text-violet-300">待处理</span>}</div><p className="mt-4 text-sm font-semibold text-white">{step.label}</p><p className="mt-2 text-xs leading-5 text-slate-500">{step.detail}</p></div>; })}</div></CardContent></Card>;
}
