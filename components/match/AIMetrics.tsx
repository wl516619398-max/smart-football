import { Activity, Gauge } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import type { MatchMetric } from "@/types/match";

export function AIMetrics({ homeTeam, awayTeam, metrics }: { homeTeam: string; awayTeam: string; metrics: MatchMetric[] }) {
  return <Card className="border-blue-500/20 bg-gradient-to-br from-[#111d3a] to-[#111827]"><CardHeader><CardTitle className="flex items-center gap-2 text-base text-white"><Gauge className="h-5 w-5 text-blue-400" />Athena 数据指标</CardTitle><div className="flex flex-wrap gap-4 text-xs text-slate-400"><span><i className="mr-1.5 inline-block h-2 w-2 rounded-full bg-blue-400" />{homeTeam}</span><span><i className="mr-1.5 inline-block h-2 w-2 rounded-full bg-violet-400" />{awayTeam}</span></div></CardHeader><CardContent className="space-y-5">{metrics.map((metric) => <div key={metric.label}><div className="mb-2 flex items-center justify-between text-xs"><span className="flex items-center gap-1.5 text-slate-400"><Activity className="h-3.5 w-3.5 text-blue-400" />{metric.label}</span><span className="font-semibold text-slate-200">{metric.home} <span className="px-1 text-slate-600">/</span> {metric.away}</span></div><div className="grid grid-cols-2 gap-2"><div className="h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-blue-500 transition-all" style={{ width: `${metric.home}%` }} /></div><div className="h-2 overflow-hidden rounded-full bg-slate-800"><div className="ml-auto h-full rounded-full bg-violet-500 transition-all" style={{ width: `${metric.away}%` }} /></div></div></div>)}</CardContent></Card>;
}
