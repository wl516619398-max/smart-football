import { Filter, Search, SlidersHorizontal } from "lucide-react";
import { featured } from "@/data/matches";
import { MatchCard } from "@/components/match-card";
import { Button } from "@/components/ui/button";

export default function MatchesPage() {
  return <div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><div className="flex items-center gap-2 text-xs font-medium text-blue-400"><Filter className="h-3.5 w-3.5" />MATCH CENTER</div><h1 className="mt-2 text-3xl font-semibold text-white">比赛列表</h1><p className="mt-2 text-sm text-slate-400">浏览赛程，获取每一场比赛的 AI 预测与数据洞察。</p></div><Button variant="outline"><SlidersHorizontal className="mr-2 h-4 w-4" />筛选条件</Button></div><div className="mt-8 flex flex-col gap-3 sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-lg border border-slate-800 bg-[#111827] px-3 py-2.5 text-sm text-slate-500"><Search className="h-4 w-4" />搜索球队或联赛...</div><div className="flex gap-2 overflow-x-auto"><Button variant="secondary" size="sm">全部</Button><Button variant="outline" size="sm">英超</Button><Button variant="outline" size="sm">西甲</Button><Button variant="outline" size="sm">德甲</Button></div></div><div className="mt-8"><div className="mb-4 flex items-center justify-between"><p className="text-sm font-medium text-slate-300">今日推荐 <span className="ml-1 text-slate-500">{featured.length} 场比赛</span></p><p className="text-xs text-slate-500">按开赛时间排序</p></div><div className="grid gap-4 lg:grid-cols-3">{featured.map((match) => <MatchCard key={match.id} match={match} />)}</div></div><div className="mt-12 rounded-xl border border-dashed border-slate-800 p-8 text-center"><p className="text-sm font-medium text-slate-300">更多比赛即将上线</p><p className="mt-1 text-xs text-slate-500">当前 MVP 使用 Mock Data，后续将接入实时赛程。</p></div></div>;
}
